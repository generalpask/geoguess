{
  "functions": {
    "predeploy": [
      "yarn run lint --prefix \"$RESOURCE_DIR\"",
      "yarn run build --prefix \"$RESOURCE_DIR\""
    ],
    "source": "functions"
  },
  "hosting": {
    "public": "dist",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ]
  }
}
